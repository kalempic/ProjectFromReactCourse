import { Fragment } from "react";

const Header=props=>{
    return <Fragment>
        <header>
            <h1>ReactMeals</h1>
            <button>Cart</button>
            <div>
                <img/>
            </div>
        </header>
    </Fragment>
};

export default Header;